#include "risk_engine.h"
#include <stdio.h>

int evaluate_risk(const char *username) {
    // Placeholder: real risk logic will go here
    printf("[INFO] Risk engine evaluated user: %s\n", username);
    return 0; // 0 = low risk
}